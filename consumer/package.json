{
  "name": "consumer",
  "version": "1.0.0",
  "description": "Consumer service that calls provider APIs",
  "main": "index.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --testTimeout=10000"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@pact-foundation/pact": "^13.0.0",
    "jest": "^29.7.0"
  }
}
